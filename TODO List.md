# Final touches Todo list:
- Add a usage guide modal to the workspace footer explaining things like the best practices to improve PDF output, what to expect from certain framework and CSS conbos, the guideline toggle and how to use it, tips to ensure the generated PDF stays as a single page and all that. It should be just like the keyboard shortcuts modal.
- Ensure Photo url is loaded into the save resume modal if there is a url set already. Presently the field is cleared when I refresh the page.
- On the templates library page, since the description is truncated at the bottom, make it so that the description also displays below the actions buttons when the resume is hovered.
- Fix the usage guidiance and keyboard shortcuts toggle by separating concerns in a neat way that doesnt duplucate code, and include comments as instructions on how to add a new modal with a shortcut key.
- Right now it supports only single page PDFs, can it be made to support multiple page PDF?